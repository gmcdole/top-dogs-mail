# Patches

Additive modifications applied to Thunderbird's upstream source to enable per-organization branding (and any other customizations). Each patch should be minimal and additive — existing behavior for anyone without a custom entry should remain unchanged, matching the approach already proven on the mailcow side of this project.
